<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">@color/colorPrimaryDark</item>
        <item name="android:editTextColor">@color/colorPrimaryDark</item>
        <item name="android:editTextPreferenceStyle">@style/Base.Widget.AppCompat.EditText</item>
        <item name="windowActionBar">true</item>
    </style>

    <style name="ButtonFont" parent="@android:style/TextAppearance.Holo.Widget.Button">
        <item name="android:textColor">#888888</item>
        <item name="android:text">20sp</item>
        <item name="android:textAllCaps">true</item>
    </style>

</resources>
